name: "setup-bruin"
description: >
  Install bruin for use in other jobs.
branding:
  icon: play
  color: blue
inputs:
  version:
    description: The version of bruin to set up.
    required: false
    default: 'latest'
runs:
  using: "node20"
  main: "./dist/main.js"
